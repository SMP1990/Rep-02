# Copy this file to config/.env and fill in real values for the target
# environment. config/.env is git-ignored and must never be committed.

APP_NAME="Media Utility Platform"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://example.com
APP_TIMEZONE=UTC

DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
